changelog_path = "CHANGELOG.md"
pre_bump_hooks = [
    "cargo bump %version",
    "cargo clippy",
    "cargo build --release",
    "cargo fmt --all",
]
post_bump_hooks = []
authors = [{ signature = "Kan-Ru Chen", username = "kanru" }]

[commit_types]
